import React from 'react';
import {Container} from "react-bootrap";
import Saved from "./Saved";

function Book() {
    return (
        <Container>
            <Saved />
        </Container>
    )
}

export default Book;